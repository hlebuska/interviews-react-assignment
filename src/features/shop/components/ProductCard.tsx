
            aria-label="add"
            size="small"
            onClick={() => onAddToCart(1)} // Only pass quantity
          >
            <AddIcon fontSize="small" />
          </IconButton>
        </CardActions>
      </Card>
    </Grid>
  );
}